meta {
  name: deleteProductOne
  type: http
  seq: 3
}

delete {
  url: {{root_url}}/products/{{productOneId}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 204
}

settings {
  encodeUrl: true
  timeout: 0
}
