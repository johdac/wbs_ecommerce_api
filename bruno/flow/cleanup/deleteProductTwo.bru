meta {
  name: deleteProductTwo
  type: http
  seq: 4
}

delete {
  url: {{root_url}}/products/{{productTwoId}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 204
}

settings {
  encodeUrl: true
  timeout: 0
}
