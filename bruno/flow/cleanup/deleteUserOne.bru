meta {
  name: deleteUserOne
  type: http
  seq: 2
}

delete {
  url: {{root_url}}/users/{{userOneId}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 204
}

settings {
  encodeUrl: true
  timeout: 0
}
