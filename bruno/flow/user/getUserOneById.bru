meta {
  name: getUserOneById
  type: http
  seq: 5
}

get {
  url: {{root_url}}/users/{{userOneId}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
