meta {
  name: deleteUserTwo
  type: http
  seq: 7
}

delete {
  url: {{root_url}}/users/{{userTwoID}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 204
}

settings {
  encodeUrl: true
  timeout: 0
}
