meta {
  name: updateOrderOne
  type: http
  seq: 6
}

put {
  url: {{root_url}}/orders/{{orderOneId}}
  body: json
  auth: inherit
}

body:json {
  {
    "userId": "{{userOneId}}",
    "products": [
      {
        "productId": "{{productOneId}}",
        "quantity": 2
      }
    ]
  }
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
