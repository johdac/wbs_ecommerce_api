meta {
  name: createOrderTwo
  type: http
  seq: 2
}

post {
  url: {{root_url}}/orders
  body: json
  auth: inherit
}

body:json {
  {
    "userId": "{{userOneId}}",
    "products": [
      {
        "productId": "{{productOneId}}", 
        "quantity": 4
      }
    ]}
}

vars:post-response {
  orderTwoId: res.body.id
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
