meta {
  name: getOrderOneById
  type: http
  seq: 4
}

get {
  url: {{root_url}}/orders/{{orderOneId}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
