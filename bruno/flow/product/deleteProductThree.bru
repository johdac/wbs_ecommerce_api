meta {
  name: deleteProductThree
  type: http
  seq: 9
}

delete {
  url: {{root_url}}/products/{{productThreeId}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 204
}

settings {
  encodeUrl: true
  timeout: 0
}
