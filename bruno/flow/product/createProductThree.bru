meta {
  name: createProductThree
  type: http
  seq: 3
}

post {
  url: {{root_url}}/products
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Greatest Product 3",
    "price": 12,
    "category": "{{categoryOneId}}"
  }
}

vars:post-response {
  productThreeId: res.body.id
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
