meta {
  name: getProductOneById
  type: http
  seq: 5
}

get {
  url: {{root_url}}/products/{{productOneId}}
  body: json
  auth: inherit
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
